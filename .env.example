# Database connection string (e.g., postgresql://user:password@host:port/database)
DATABASE_URL=

# SendGrid API Key for sending emails
SENDGRID_API_KEY=

# Secret key for session management (choose a long, random string)
SESSION_SECRET=

# Node environment (usually 'development' or 'production')
# NODE_ENV=development

# Replit specific ID (only needed if running on Replit)
# REPL_ID=
